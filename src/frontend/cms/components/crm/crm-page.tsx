'use client';
import { useCallback, useEffect, useState } from 'react'; import { DataTable } from '../crud/data-table'; import { SearchBar } from '../crud/search-bar'; import { Pagination } from '../crud/pagination'; import { FormBuilder } from '../forms/form-builder'; import type { CrudColumn, CrudField } from '../../types/crud';
type Row={id:string;[key:string]:unknown};
export function CrmPage({resource,title,columns,fields}:{resource:string;title:string;columns:CrudColumn<Row>[];fields:CrudField<Row>[]}) { const pageSize=20; const [data,setData]=useState<Row[]>([]),[search,setSearch]=useState(''),[page,setPage]=useState(1),[total,setTotal]=useState(0),[values,setValues]=useState<Record<string,unknown>>({}); const load=useCallback(async()=>{const response=await fetch(`/api/v1/crm/${resource}?search=${encodeURIComponent(search)}&page=${page}`); const payload=await response.json() as {data:Row[];pagination:{total:number}};setData(payload.data);setTotal(payload.pagination.total);},[page,resource,search]); useEffect(()=>{void load()},[load]); return <section className="grid gap-6"><h1 className="text-2xl font-bold">{title}</h1><SearchBar value={search} onChange={(value)=>{setSearch(value);setPage(1)}} placeholder="Buscar"/><DataTable columns={columns} data={data} getRowId={(row)=>row.id}/><Pagination page={page} pageCount={Math.ceil(total/pageSize)} onPageChange={setPage}/><FormBuilder fields={fields} values={values} onChange={(name,value)=>setValues((current)=>({...current,[name]:value}))} onSubmit={async()=>{await fetch(`/api/v1/crm/${resource}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(values)});setValues({});await load();}}/></section> }
