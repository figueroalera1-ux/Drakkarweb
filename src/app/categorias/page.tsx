import Link from 'next/link';import {categories} from '@frontend/site/content';export default function Page(){return <main className="mx-auto max-w-7xl px-5 py-16"><h1 className="text-5xl">Categorías</h1><div className="mt-12 grid gap-6 md:grid-cols-3">{categories.map(c=><Link href={`/catalogo?categoria=${c.slug}`} key={c.slug}><img src={c.image} alt={c.name} className="aspect-[4/5] rounded-2xl object-cover"/><h2 className="mt-4 text-2xl">{c.name}</h2></Link>)}</div></main>}
